# tuples () are different from lists[] because:
# 1) tuples are immutable;
# 2) you can create a list of tuples;

coordinates = (4, 5)
print(coordinates)
print(coordinates[0])

coordinates_many = [(4,5), (6,7), (8,9)]
